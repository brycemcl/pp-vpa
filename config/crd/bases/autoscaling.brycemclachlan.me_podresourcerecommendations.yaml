---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: podresourcerecommendations.autoscaling.brycemclachlan.me
spec:
  group: autoscaling.brycemclachlan.me
  names:
    categories:
    - autoscaling
    kind: PodResourceRecommendation
    listKind: PodResourceRecommendationList
    plural: podresourcerecommendations
    shortNames:
    - prr
    singular: podresourcerecommendation
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodResourceRecommendation is the Schema for the podresourcerecommendations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PodResourceRecommendationSpec links the recommendation to
              a specific pod.
            properties:
              targetPodName:
                description: TargetPodName is the pod this PRR shadows.
                type: string
            required:
            - targetPodName
            type: object
          status:
            description: PodResourceRecommendationStatus is the recommender output
              and checkpoint.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              containerRecommendations:
                items:
                  description: ContainerRecommendation is the per-container output
                    of the recommender.
                  properties:
                    containerName:
                      type: string
                    lowerBound:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: ResourceList is a set of (resource name, quantity)
                        pairs.
                      type: object
                    target:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: ResourceList is a set of (resource name, quantity)
                        pairs.
                      type: object
                    uncappedTarget:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: UncappedTarget exposes the pure calculation pre-clamping.
                      type: object
                    upperBound:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: ResourceList is a set of (resource name, quantity)
                        pairs.
                      type: object
                  required:
                  - containerName
                  type: object
                type: array
              contentionHighWatermarks:
                description: ContentionHighWatermarks records decayed per-PRR high-watermarks.
                properties:
                  containerLevel:
                    items:
                      description: ContainerLevelContention captures per-container
                        contention high-watermarks.
                      properties:
                        containerName:
                          type: string
                        cpu:
                          type: string
                        lastUpdated:
                          format: date-time
                          type: string
                        memory:
                          type: string
                      required:
                      - containerName
                      type: object
                    type: array
                  podLevel:
                    description: PodLevelContention captures pod-level contention
                      high-watermarks.
                    properties:
                      cpu:
                        type: string
                      lastUpdated:
                        format: date-time
                        type: string
                      memory:
                        type: string
                    type: object
                type: object
              histogramCheckpoint:
                description: |-
                  HistogramCheckpoint is the base64-encoded gob of the node-agent's
                  decaying histogram for this pod.
                type: string
              observedPeak:
                description: ObservedPeak captures absolute peaks (memory floor enforcement).
                properties:
                  containers:
                    items:
                      description: ObservedPeakContainer is the absolute peak observed
                        for a single container.
                      properties:
                        containerName:
                          type: string
                        memory:
                          type: string
                      required:
                      - containerName
                      type: object
                    type: array
                  podLevel:
                    description: ObservedPeakPodLevel is the absolute peak observed
                      at the pod level.
                    properties:
                      memory:
                        type: string
                    type: object
                type: object
              podLevelTarget:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: ResourceList is a set of (resource name, quantity) pairs.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
