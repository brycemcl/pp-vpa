apiVersion: v1
kind: Service
metadata:
  name: pp-vpa-metrics
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: pp-vpa
    app.kubernetes.io/component: manager
spec:
  selector:
    app.kubernetes.io/name: pp-vpa
    app.kubernetes.io/component: manager
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
